
template "control_options"
    node "bar"
        apply "tab_button"
            script
        node "tab_button"

        node
        node
        node
        node
        node
        node
        node


node "control_options"
    template "control_options"
